/* import { ReactNode, createContext } from 'react'
import { LoadScript } from '@react-google-maps/api'

const MapLoadContext = createContext({})

const LoadMap = ({ children }: { children: ReactNode }) => {
  const {} = LoadScript({}) 

  return <MapLoadContext.Provider></MapLoadContext.Provider>
}
*/
